'use strict';

var QuantitySelector = require('../quantity-selector');

describe('QuantitySelector View', function() {

  beforeEach(function() {
    this.quantitySelector = new QuantitySelector();
  });

  it('Should run a few assertions', function() {
    expect(this.quantitySelector);
  });

});
